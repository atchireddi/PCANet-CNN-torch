## push an exist repo to github
# git init
# git add .
# git commit -m "Initial commit"
# git remote add origin <project url>
# git push -f origin master


# git pull origin master
git add .
git commit -m "commit"
git push origin master
